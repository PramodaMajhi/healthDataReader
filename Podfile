platform :ios, '9.0'

target 'Health Data Reader-Writer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Health Data Reader-Writer
pod 'Canvas'
pod 'ProgressHUD'

end
